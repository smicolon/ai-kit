# Archived Project Templates

> **Status: Archived**
> These static template directories (`django-project`, `nestjs-project`, `nextjs-project`, `nuxtjs-project`) and `.claude-project.json` configurations represent an early prototype of project-level conventions (October 2025).
>
> In current versions of `@smicolon/ai-kit`, conventions are dynamically installed via:
> 1. Claude Code Marketplace: `/plugin install <pack>`
> 2. Cross-tool CLI: `bunx @smicolon/ai-kit add <pack>` (or `ai-kit init`)
>
> They are preserved here in `archive/` for historical reference per repository archive policy.
